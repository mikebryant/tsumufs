# Automatically added by dh_pycentral
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall tsumufs
	if grep -qs '^tsumufs$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^tsumufs$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
